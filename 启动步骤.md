# 脑护通 Demo 启动步骤（Windows）

本文适用于在另一台 Windows 电脑上下载、更新并运行本项目。

## 一、准备环境

请先安装：

- Git：https://git-scm.com/download/win
- Node.js 20 或更高版本：https://nodejs.org/

安装完成后打开 PowerShell，检查版本：

```powershell
git --version
node --version
npm --version
```

## 二、首次下载项目

```powershell
git clone https://github.com/RIverJiaH/ASH-DCI-Play-8.git
cd ASH-DCI-Play-8
```

如果电脑中已经有旧仓库，请执行：

```powershell
cd 你的仓库目录\ASH-DCI-Play-8
git fetch origin
git switch main
git pull origin main
```

如果提示本地有未提交修改，可以先备份修改：

```powershell
git stash push -u -m "本地修改备份"
git pull origin main
```

需要恢复备份时执行：

```powershell
git stash pop
```

## 三、安装依赖

进入前端目录并安装依赖：

```powershell
cd frontend
npm install
```

首次安装可能需要几分钟，请等待命令正常结束。

## 四、配置 AI

### 方案 A：使用模拟 AI

在 `frontend` 目录新建 `.env.local`，内容如下：

```env
AI_OPTIONS_MODE=mock
AI_OPTIONS_MODEL=mock-ai-v1
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_API_KEY=
DEVICE_ADAPTER=disabled
HOME_ASSISTANT_URL=
HOME_ASSISTANT_TOKEN=
```

模拟 AI 不需要密钥，适合离线演示。

### 方案 B：使用真实 DeepSeek

在 `frontend` 目录新建 `.env.local`，内容如下：

```env
AI_OPTIONS_MODE=deepseek
AI_OPTIONS_MODEL=deepseek-chat
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_API_KEY=填写你自己的DeepSeek密钥
DEVICE_ADAPTER=disabled
HOME_ASSISTANT_URL=
HOME_ASSISTANT_TOKEN=
```

不要把真实 API 密钥提交到 Git。项目已默认忽略 `.env.local`。

## 五、启动系统

回到项目根目录：

```powershell
cd ..
```

双击运行：

```text
start_local_demo.bat
```

也可以在 `frontend` 目录手动启动：

```powershell
npm run dev -- --hostname 0.0.0.0 --port 8000
```

启动窗口必须保持打开。按 `Ctrl+C` 可以停止系统。

## 六、本机访问地址

- 患者端：http://localhost:8000/?view=patient
- 护理端：http://localhost:8000/?view=nurse

患者提交需求后，护理端会在约 1 秒内自动同步显示。

## 七、两台电脑通过局域网使用

患者端和护理端不需要分别启动两套服务。只在一台电脑上运行系统，两台电脑访问同一个服务地址即可。

在运行服务的电脑上查看 IPv4 地址：

```powershell
ipconfig
```

找到当前 Wi-Fi 或以太网的“IPv4 地址”，例如 `192.168.1.3`。

电脑 1 打开患者端：

```text
http://192.168.1.3:8000/?view=patient
```

电脑 2 打开护理端：

```text
http://192.168.1.3:8000/?view=nurse
```

请确保：

- 两台电脑连接同一个局域网；
- 运行服务的电脑没有关闭启动窗口；
- Windows 防火墙允许 TCP 8000 端口进入；
- 地址中的 IP 使用运行服务电脑当前的 IPv4 地址。

如需手动放行端口，请使用管理员 PowerShell：

```powershell
New-NetFirewallRule -DisplayName "Brain Care Demo LAN 8000" -Direction Inbound -Action Allow -Protocol TCP -LocalPort 8000 -Profile Private
```

## 八、通过 cpolar 公网访问

电脑中已经安装并登录 cpolar 时，在项目根目录双击：

```text
start_public_demo.bat
```

cpolar 隧道需转发到：

```text
http://localhost:8000
```

假设 cpolar 分配的地址为：

```text
https://你的地址.cpolar.top
```

则三个独立入口为：

- SAH-DCI预警总览：`https://你的地址.cpolar.top/?view=dci`
- 患者端：`https://你的地址.cpolar.top/?view=patient`
- 护理端：`https://你的地址.cpolar.top/?view=nurse`

免费 cpolar 地址可能在隧道重启后变化，请以当次控制台显示的 HTTPS 地址为准。

## 九、验证是否启动成功

在浏览器访问：

```text
http://localhost:8000/api/demo
```

如果看到 JSON 数据，说明后端接口正常。

也可以在 `frontend` 目录执行完整检查：

```powershell
npm run build
npm test
```

## 十、常见问题

### 端口 8000 被占用

查看占用进程：

```powershell
Get-NetTCPConnection -LocalPort 8000 -ErrorAction SilentlyContinue
```

关闭旧的系统启动窗口后重新运行。

### 护理端看不到患者提交的任务

确认两端地址中的域名/IP 和端口完全相同，只允许 `view` 参数不同。不要让两台电脑各自启动一套服务，否则两套服务的数据不会同步。

### DeepSeek 没有显示实时分析

检查 `.env.local` 是否为 `AI_OPTIONS_MODE=deepseek`，密钥是否有效，然后停止并重新启动系统。

### 公网地址打不开

确认本地系统和 cpolar 服务都在运行，并在 cpolar 管理页面确认隧道目标端口是 `8000`。

## 十一、赛道7录制演示顺序

启动后优先打开：

```text
http://localhost:8000/?view=dci
```

推荐按以下顺序录制，所有数据都可用“重置全部演示”恢复：

1. 在“患者总览”介绍系统边界、三位模拟患者和风险积分阶梯。
2. 选择患者 A（1分），展示绿色低风险、趋势稳定和多模态证据链，说明系统不会因正常波动无故报警。
3. 进入“EEG / qEEG”，展示 OpenBCI 接入、8通道质量、原始/滤波波形和各项qEEG指标。
4. 选择患者 B（4分），展示第3天 alpha、ADR、PAV下降及黄色预警。
5. 进入“AI解释”，点击“运行AI分析”，展示固定五段式输出。
6. 进入“医护闭环”，完成护士四项复核、医生确认和复核方向选择。
7. 选择患者 C（8分），展示第5天 ADR下降、delta上升、半球不对称扩大和红色预警。
8. 对患者 C 完成复核，点击“播放处置后趋势”，展示积分从8分降到3分且原事件保留。
9. 进入“事件报告”生成报告，再到“验证计划”，最后回到患者总览。

完整逐镜头步骤见项目根目录的 `赛道7录制演示步骤.md`。
